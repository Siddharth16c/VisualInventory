COMPLETED THIS SESSION ✅
[x] T1 — Items schema additions (keyword_id, reorder_threshold, FTS, indexes)
[x] T2 — stock_movements table + RLS + RPCs
[x] T3 — 3-layer location system (storage_places/zones/slots/item_locations)
[x] T4 — RLS hardening on all existing tables
[x] T5 — dal.ts full rewrite + bug fixes + new methods
[x] T6t — types.ts additions (new interfaces)
[x] T7b — Billing.tsx refactor (server search, atomic stock, hotkeys)
[x] T9 — DBEditor Page 1 (table view + query constructor + named controls)

PENDING — YOUR QUEUE 📋
[ ] T-RPC1 — Run in Supabase: backfill_keyword_ids() + 2 RPCs from schema tasks
[ ] T-RPC2 — Run in Supabase: run_readonly_query() RPC for DBEditor
[ ] T10 — DBEditor Page 2 (controls library page — fire buttons per table) → paste when ready

[ ] T6s — Subcategories SQL + normalization (small, standalone)
[ ] T7d — MinIO + Directus Docker setup guide
[ ] T8ts — Typesense sync setup
[ ] T11 — TanStack Query integration
[ ] T12 — Analytics.tsx KPI + heatmaps → paste current file
[ ] T13 — Location UI (SVG floor plans + item placement)
[ ] T14 — Grafana dashboard setup
[ ] T15 — Inkscape SVG diagrams
[ ] T16 — n8n workflows
[ ] T17 — CCTV voxel sync (LAST)


General Idea/plan(major changes):
__________________________________________________________________________
- Stock updates - movement, sale(bill logged/offline sale)
- purchase analysis -
	- purchase history - rows of purchasing - items, qty, date, amt. -> grouped under supplier & domain
	- split screen viewer.
	- metrics - investments + costs (parameter filters - domain, monthly for fast selling domain items, yearly for seasonable)
- metrics - 
	- Investments + total sales + costs(factor distribution visuals)
	- Stock remains = stock sold - stock bought + stock present before 
- billing/accounting (separate API/frontend layer)- 
	- search/filter page -> thumbnail icon for every item
	- invoice generation - A4(proper format) + receipt print (thermal printing)
	- Pending payments - client list(date reminder setting)
	- bill delete/edit option after saving- delete items - add back to stock.
__________________________________________________________________________
- Stock positions detector -
	- Draw top views of each floor(sectioned logically for easy organization), ladders for all storing places(KT shop, shop n2, small warehouse, etc)
	- Add/create storing place - draw top views - sections and mark db fields values -> move/add/remove(sale) items
	- Multiple storing place views - open/click on 1 -> displays all items with qty placed there(group by that stored place).
	- A panel of selection of items between 2 stored placed 
	- [[stock position UI Idea]]
__________________________________________________________________________
Store everything - 10m items of 10-12 categories
- (SEARCH* FILTERS - type sense + new DB schema + tanstack query + READ UI -> location based READ -> click on the section/floor & get items placed there, move items between-> stores/floors/sections )check stock availability, price, location(high-level) -
- stock threshold limit for restocking alert
- location detector - which store> which floor > which section > and how much?
- stock direct search postgrest full tsvector
	- Displayed group by domain>products(generic)
	- 1 factor as label/flag on items of domains - "Prioritised selling" for old/dead stock - separate read panel under title - "Priority target"
- Media sharing - 
	- multiple images saved per item
	- catalogue/price generator -> html file

_________________________________________________________________________________________________

Tech stack:
- DB - Supabase (Postgres + RLS + tsvector) 
- Search  Typesense (primary) + PG FTS (fallback) + tan-stack(client side)
- Automation - n8n self-hosted  
- Frontend - Your existing React/TS/Vite + Zustand
- DB Admin UI - Custom React admin (named-button query UI) 
- Directus - separation of features and custom allowance to accounts, data layer + admin UI, DB UI + bulk operations(insert),
	- Roles and feature distribution
		- Staff - Stock search, billing, location view
		- Owners - Purchase features, prospect data, media
		- You only - All metrics, full DB access, system config
- n8n - automation, triggers, workflows, cross service logic
- KPI Dashboards - Grafana → Supabase direct 
- Floor Heatmaps | D3 in React (you already have it)
- Invoicing - jsPDF (you already have it) 
- Diagrams - Inkscape SVG, versioned in your repo 